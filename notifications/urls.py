from django.urls import path
from . import views

urlpatterns = [
    # Aquí puedes agregar rutas como:
    # path('', views.list_notifications, name='list_notifications'),
    # path('send/', views.send_notification, name='send_notification'),
]
